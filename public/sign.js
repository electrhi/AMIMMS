function setup(id){const c=document.getElementById(id);const ctx=c.getContext('2d');let draw=false;function resize(){const r=c.getBoundingClientRect();const img=c.toDataURL();c.width=r.width*devicePixelRatio;c.height=r.height*devicePixelRatio;ctx.scale(devicePixelRatio,devicePixelRatio);ctx.lineWidth=2.5;ctx.lineCap='round';ctx.strokeStyle='#111';if(img.length>100){const im=new Image();im.onload=()=>ctx.drawImage(im,0,0,r.width,r.height);im.src=img}}function pos(e){const r=c.getBoundingClientRect();const p=e.touches?e.touches[0]:e;return{x:p.clientX-r.left,y:p.clientY-r.top}}function start(e){draw=true;const p=pos(e);ctx.beginPath();ctx.moveTo(p.x,p.y)}function move(e){if(!draw)return;e.preventDefault();const p=pos(e);ctx.lineTo(p.x,p.y);ctx.stroke()}function end(){draw=false}resize();window.addEventListener('resize',resize);c.addEventListener('mousedown',start);c.addEventListener('mousemove',move);c.addEventListener('mouseup',end);c.addEventListener('mouseleave',end);c.addEventListener('touchstart',start,{passive:false});c.addEventListener('touchmove',move,{passive:false});c.addEventListener('touchend',end);return c}const giver=setup('giverCanvas');const receiver=setup('receiverCanvas');function clearCanvas(id){const c=document.getElementById(id);c.getContext('2d').clearRect(0,0,c.width,c.height)}function blank(c){const d=c.getContext('2d').getImageData(0,0,c.width,c.height).data;for(let i=3;i<d.length;i+=4){if(d[i]!==0)return false}return true}function saveSigns(){if(blank(giver)){alert('주는 사람 서명을 입력해주세요.');return false}if(blank(receiver)){alert('받는 사람 서명을 입력해주세요.');return false}document.getElementById('giver_sign').value=giver.toDataURL('image/png');document.getElementById('receiver_sign').value=receiver.toDataURL('image/png');return true}